export default defineAppConfig({
  ui: {
    colors: {
      primary: 'violet',
      neutral: 'neutral'
    },
    card: {
      defaultVariants: {
        variant: 'outline'
      }
    },
    input: {
      defaultVariants: {
        variant: 'soft',
        color: 'secondary'
      }
    },
    select: {
      defaultVariants: {
        variant: 'soft',
        color: 'secondary'
      }
    },
    textarea: {
      defaultVariants: {
        variant: 'soft',
        color: 'secondary'
      }
    },
    selectMenu: {
      defaultVariants: {
        variant: 'soft',
        color: 'secondary'
      }
    },
    inputMenu: {
      defaultVariants: {
        variant: 'soft',
        color: 'secondary'
      }
    },
    inputNumber: {
      defaultVariants: {
        variant: 'soft',
        color: 'secondary'
      }
    },
    inputTags: {
      defaultVariants: {
        variant: 'soft',
        color: 'secondary'
      }
    },
    inputDate: {
      defaultVariants: {
        variant: 'soft',
        color: 'secondary'
      }
    },
    inputTime: {
      defaultVariants: {
        variant: 'soft',
        color: 'secondary'
      }
    },
    pinInput: {
      defaultVariants: {
        variant: 'soft',
        color: 'secondary'
      }
    }
  }
})
